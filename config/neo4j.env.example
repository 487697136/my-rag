# Neo4j 环境变量配置文件
# 
# 使用方法:
# 1. 复制此文件为 .env: cp config/neo4j.env.example .env
# 2. 根据您的环境修改下面的配置
# 3. 在Python中加载: from dotenv import load_dotenv; load_dotenv()

# ==========================================
# Neo4j 数据库连接配置 (Neo4j Aura)
# ==========================================

# Neo4j Aura实例地址
NEO4J_URI=neo4j+s://47aa7a59.databases.neo4j.io

# Neo4j 认证信息
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=1COh0KFBOJL12ByoSj82D0eoeXpFv8bWWvocts83m9s

# Neo4j 数据库名称
NEO4J_DATABASE=neo4j

# ==========================================
# Neo4j Aura 实例信息
# ==========================================

# Aura实例ID
AURA_INSTANCEID=47aa7a59

# Aura实例名称
AURA_INSTANCENAME=Instance01

# ==========================================
# Neo4j 高级配置 (可选)
# ==========================================

# 连接池大小
NEO4J_MAX_CONNECTION_POOL_SIZE=50

# 连接超时时间 (秒)
NEO4J_CONNECTION_TIMEOUT=30

# 事务重试时间 (秒)  
NEO4J_MAX_TRANSACTION_RETRY_TIME=30

# ==========================================
# GraphRAG 配置 (可选)
# ==========================================

# 工作目录
GRAPHRAG_WORKING_DIR=./neo4j_graphrag_cache

# 是否启用LLM缓存
GRAPHRAG_ENABLE_LLM_CACHE=true

# 最大token数量配置
GRAPHRAG_BEST_MODEL_MAX_TOKEN_SIZE=4000
GRAPHRAG_CHEAP_MODEL_MAX_TOKEN_SIZE=2000

# 嵌入批次大小
GRAPHRAG_EMBEDDING_BATCH_NUM=32

# ==========================================
# 示例配置说明
# ==========================================

# 1. Neo4j Desktop (推荐)
# NEO4J_URI=neo4j://127.0.0.1:7687
# NEO4J_USERNAME=neo4j
# NEO4J_PASSWORD=neo4j123456

# 2. Docker 环境
# NEO4J_URI=bolt://localhost:7687
# NEO4J_USERNAME=neo4j  
# NEO4J_PASSWORD=neo4j123456

# 3. Neo4j Aura云服务
# NEO4J_URI=neo4j+s://xxxxx.databases.neo4j.io
# NEO4J_USERNAME=neo4j  
# NEO4J_PASSWORD=your-aura-password

# 4. 生产环境
# NEO4J_URI=bolt://production-server:7687
# NEO4J_USERNAME=your-username
# NEO4J_PASSWORD=your-secure-password 